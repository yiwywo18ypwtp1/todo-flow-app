[build]
builder = "nixpacks"
buildCommand = "cd backend && poetry install --no-interaction --no-ansi"

[deploy]
startCommand = "cd backend && poetry run uvicorn app.main:app --host 0.0.0.0 --port 8000"
