export type SignupUser = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}

export type LoginUser = {
    email: string;
    password: string;
}

export type User = {
    firstName: string;
    lastName: string;
    email: string;
}